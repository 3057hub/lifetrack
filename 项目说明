# LifeTrack - 个人时间追踪与 AI 复盘助手

## 项目概述

这是一个个人日常使用的手机端网页工具（PWA），用来记录"几点到几点做了什么"，并定期通过 DeepSeek API 分析行为模式，给出迭代优化建议。AI 具备记忆能力——每次分析后会自动提取摘要，下次分析时结合用户的反馈和历史记忆，让建议越来越贴近用户的实际情况。

使用场景：手机浏览器打开 → 点一下"开始"，放下手机做事 → 做完点"结束"，输入做了什么 → 每周生成一份 AI 分析报告，看看时间都去哪了、怎么改进。

## 技术约束（硬性要求，不可改变）

- **后端**：Python 3.10+, FastAPI, SQLAlchemy ORM, SQLite, httpx（异步调用 DeepSeek API）
- **前端**：单个 HTML 文件，Alpine.js（CDN 引入）实现交互，纯 CSS（移动端优先），支持 PWA
- **部署**：2核2G 阿里云服务器，Nginx 反代 /api 到 FastAPI（端口 8000），静态文件由 Nginx 直接服务
- **禁止使用**：React、Vue、Next.js 等前端框架；Expo、React Native；MySQL、PostgreSQL

核心原则：**极简单文件架构，手机优先，功能完整但不过度工程化**。

## 功能需求

### 1. 活动记录（两种方式）

**快捷计时器**（推荐的主要使用方式）：
- 首页顶部放一个大号计时按钮，点击"开始"开始计时，点击"结束"弹窗输入"正在做什么"并确认结束时间
- 计时期间实时显示已经过了多久（前端本地计算，但以服务器时间为准）
- 关闭浏览器再打开时，能恢复计时状态（如果还有进行中的活动）
- 计时开始和结束的时间戳都以服务器端为准，避免手机休眠导致偏差

**手动补录**：
- 表单输入：开始时间、结束时间、活动描述、可选标签（逗号分隔）
- 用于事后补录忘记计时的事情

### 2. 历史记录

- 按日期分组的时间轴列表
- 每条记录可编辑（修改时间、描述、标签）和删除
- 支持按日期筛选

### 3. AI 分析报告

- 用户点击"生成本周分析"或"生成今日总结"
- 系统收集指定周期内的活动记录 + 当前目标 + 历史记忆 + 用户对上次建议的反馈
- 调用 DeepSeek API（模型: deepseek-chat），生成结构化分析报告（Markdown 格式）
- 报告包含：行为模式总结、时间黑洞识别、2-3 条可执行的改进建议
- AI 返回的 JSON 中包含完整报告和一份不超过 300 字的记忆摘要
- 如果 JSON 解析失败，需要重试或给出友好错误提示

### 4. 记忆系统

- 每次 AI 生成报告时，自动提取一份 ≤300 字的纯文本记忆摘要
- 用户可对报告给出反馈（有帮助 / 已执行 / 忽略）
- 下次分析时，将最近的记忆和反馈注入 Prompt，实现建议的迭代进化
- 后端需要提供更新报告反馈的 API

### 5. 目标设置

- 用户可设定当前优化目标（如"每天深度工作 3 小时"）
- AI 分析时会参考这些目标
- 目标可激活和停用（保留历史，不删除）

### 6. 简单密码保护

- 环境变量中设置访问密码 `APP_PASSWORD`
- 前端首次访问时弹出密码输入框，验证通过后存入 localStorage
- 后续请求在 Header 中携带密码（或 token），后端校验
- 不需要完整的用户注册/登录系统，只是一个简单的访问控制

### 7. PWA 体验

- 添加到手机主屏幕后可独立打开（display: standalone）
- 提供应用图标（192x192 和 512x512）
- 简单的 Service Worker 缓存静态资源，支持离线展示缓存页面

## 开发分阶段执行

### 阶段 1：核心功能 + 手机界面

先搭建可用版本，包含：
- 活动记录（计时器 + 手动补录）的完整前后端
- 历史记录查看、编辑、删除
- 手机端 UI（三个页面：记录、历史、报告）+ 底部导航栏
- PWA 配置（manifest + Service Worker + 图标占位）
- 密码保护

阶段 1 完成后应该能**在手机上正常记录和查看活动、添加到主屏幕**。

### 阶段 2：AI 大脑

在阶段 1 稳定后加入：
- DeepSeek API 分析报告生成
- 记忆系统（报告摘要提取 + 反馈更新）
- 报告页面（Markdown 渲染 + 反馈按钮）
- 目标设置页面和 API

## 部署要求

- 所有代码部署在阿里云 `/opt/lifetrack` 目录
- uvicorn 运行 FastAPI（建议 systemd 管理进程）
- Nginx 反代 `/api/` 到 `http://127.0.0.1:8000`，静态文件直接 serve
- 最终启用 HTTPS（Let's Encrypt）

## 交付物

请按阶段生成所有需要的代码文件，包括：
- Python 后端代码
- 单个 HTML 前端文件
- manifest.json、Service Worker、图标
- Nginx 配置文件
- requirements.txt

**现在请从阶段 1 开始搭建。**
